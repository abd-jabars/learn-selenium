1. install node js & npm
2. download webdriver & add it to PATH
3. create project directory
4. npm init
5. npm install selenium-webdriver
6. npm install -g chromedriver || npm install --save-dev chromedriver
7. to run script in terminal: $ node path/to/file.js